"""Deliberately broken file to test the lint CI failure path.

Temporary: removed once the `lint` workflow's failure has been
observed on `rockleona/modmesh:master`.
"""

print("intentional lint failure test")   

# vim: set ff=unix fenc=utf8 et sw=4 ts=4 sts=4 tw=79:
